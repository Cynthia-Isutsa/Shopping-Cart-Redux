import { createSlice } from "@reduxjs/toolkit"

const userSlice = createSlice({
    name: "users", 
    initialState: {},
    reducers: {
        
    }
})

const UserReducer = () => {
  return (
    <div>UserReducer</div>
  )
}

export default UserReducer